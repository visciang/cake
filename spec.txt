- project / namespace per qualificare i target?

- target:
    X notazione specifica per i target +mytarget
    X DAG aciclico - analisi dei FROM / COPY --from
- target alias:
    X NON possono essere referenziati in FROM/COPY
- target body
    X niente FROM **AS**
    - DAKE_COMMAND estensioni
        X @ouput
            X introdurre taget addizionale "target.output from scratch" per estrarre gli output
        - @push
            X non deve essere dipendenza di un target
            X deve essere incluso solo se invocato `--push`
            X force run via random ARG
X target output
X target docker image output (--tag)
- private targets ?: .target

- include:
  - espansione testuale semplice (come C #include <>)
  - supportare path: local, http, git
  - supportare include ricorsivi (fare detection di loop)
  - @include path/Dakefile ARG=___
  - gestire contesto di build associato alla locazione di origine
   -> necessario processare tutti i COPY (senza --from) e qualificare le <src> con il path
      del contesto originario. Il contesto deve essere copiato localmente in una subdirectory insieme al file incluso

- "service tests"
    docker compose like (build --network ?)

- run against a private buildkit instance

- cli:
    log: da --progress=plain e qualcosa di tty colorato e raggruppato per target

    USAGE:
         dake [options] command [command options]
    
    COMMANDS:
         run - Run pipeline
               --push
               --arg
         ls - List targets
               --tree
