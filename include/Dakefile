ARG INCLUDED_ARG=abc

xxx.all: xxx.target

xxx.toolchain:
    FROM alpine
    COPY .dake/xxx/file.txt .
    RUN echo "xxx.toolchain"

@output /xxx
xxx.target:
    FROM xxx.toolchain
    RUN mkdir xxx/ && echo "xxx" > /xxx/target
